var db = require('./db');

module.exports = function(path){
  var database;
  
  db(path).then(function(d){
    database = d;
  });

  return function *(next){
    var path = this.request.path;

    try{
      this.response.body = database.get(path);
    }catch(e){
      yield next;
    }
  };
}
